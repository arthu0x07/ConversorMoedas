import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js'

ReactDOM.render(
    <App />,
  document.getElementById('root')
);

{/* Componente App recebe a aplicação que se refere a página inteira montada, que vem da pasta "Pages" e aqui, renderizamos App.*/}